# Frontend environment (see also .env.local.example and .env.vercel.example)

# Production (Vercel): set to your Render API URL
VITE_API_URL=

# Local dev only: Vite proxy target
VITE_API_PROXY_TARGET=http://localhost:3001

GEMINI_API_KEY=
